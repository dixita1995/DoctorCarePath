<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->

        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="RatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/color_ad</item>
        <item name="colorControlActivated">@color/color_ad</item>
    </style>

    <style name="RatingBarAd" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/color_ad_btn</item>
        <item name="colorControlActivated">@color/color_ad_btn</item>
    </style>

</resources>
